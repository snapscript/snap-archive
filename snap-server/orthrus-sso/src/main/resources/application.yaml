server:
  directory: C:/Work/development/snapscript/snap-archive/snap-server/orthrus-sso
  port: 7070
  packages: io.orthrus.sso   
   
jmx:
   login: admin
   password: password12
   port: 5577
   
login:
   url:
      directory: http://localhost:8383/
      base: https://orthrus.io/
   access-grant:
      store: ${server.directory}/store/grant
   access-request:
      store: ${server.directory}/store/request  
   token-store:
      type: PKCS12   
      path: C:/Work/development/snapscript/snap-archive/snap-server/orthrus-gateway/certificate/www.orthrus.io/www.orthrus.io.pfx 
      alias: 1
      password: password12  
   pass-code:
      store: ${server.directory}/store/code
      high: 99999
      low: 11111
            
mail:
   host: localhost

logging:
  level:
    root: INFO        
