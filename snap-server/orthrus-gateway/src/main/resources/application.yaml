server:
  directory: c:/temp
  port: 7979
  packages: io.orthrus.gateway   
      
gateway:   
  domain: www.orthrus.io
  resources: C:/Work/development/snapscript/snap-archive/snap-server/orthrus-gateway 
  plan: ${gateway.resources}/src/main/resources/plan.json   
  key-store: ${gateway.resources}/certificate/${gateway.domain}/${gateway.domain}.pfx  

jmx:
   login: admin
   password: password12
   port: 9989
   

logging:
  level:
    root: INFO        

