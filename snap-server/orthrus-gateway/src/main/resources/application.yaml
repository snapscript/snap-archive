server:
  name: gateway
  directory: C:/Work/development/snapscript/snap-archive/snap-server/orthrus-gateway
  port: 7979
  packages: io.orthrus.gateway   
      
gateway:   
  domain: www.orthrus.io
  resources: ${server.directory} 
  plan: ${server.directory}/src/main/resources/plan.json   
  key-store: ${server.directory}/certificate/${gateway.domain}/${gateway.domain}.pfx  

jmx:
   login: admin
   password: password12
   port: 9989
   

logging:
  level:
    root: INFO        

